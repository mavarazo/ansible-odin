---
- hosts: localhost
  become: true
  vars_files:
    - vars/default.yml

  tasks:
    - import_tasks: tasks/docker.yml
      tags: docker

    - import_tasks: tasks/heimdall.yml
      tags: heimdall